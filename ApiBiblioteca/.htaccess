RewriteEngine On
# Si el archivo o carpeta solicitado existe fisicamente, se accede directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si no existe, redirige todo a index.php pasando la ruta como parametro
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
